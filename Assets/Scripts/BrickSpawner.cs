using UnityEngine;
using System.Collections;

public class BrickSpawner : MonoBehaviour {

	public GameObject brickPrefab;

	// Use this for initialization
	void Start () {
		for (int x = 1, x < 15.5, x++) {
			for (int y = 8.5, y < )
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
